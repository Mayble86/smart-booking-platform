version: "3.8"

services:
  postgres:
    image: postgres:16
    container_name: sbpdata
    ports:
      - "15432:5432"
    environment:
      POSTGRES_DB: sbpdata
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
